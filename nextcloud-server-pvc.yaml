apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nextcloud-server-storage-claim
  labels:
    app: nextcloud
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
